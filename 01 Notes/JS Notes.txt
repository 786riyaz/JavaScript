1️⃣ Closures – Functions that remember their lexical scope even after execution.
💡 Example:

function counter() {
  let count = 0;
  return () => ++count;
}
const inc = counter();
console.log(inc()); // 1
console.log(inc()); // 2

2️⃣ Hoisting – How var, let, const, and function declarations are lifted to the top of their scope.
➡️ var → undefined | let & const → Temporal Dead Zone

3️⃣ Event Loop – Understand Call Stack, Microtasks, and Macrotasks.
💡 Example Output:

console.log('Start');
setTimeout(() => console.log('Timeout'), 0);
Promise.resolve().then(() => console.log('Promise'));
console.log('End');
// Output: Start → End → Promise → Timeout

4️⃣ Promises & Async/Await – Handle async tasks like a pro; avoid callback hell and race conditions.

5️⃣ Scope – Know Global, Function, and Block Scope — and how closures fit in.

6️⃣ This Keyword – Changes with function type and call context.
💡 Example:

const obj = {
  name: 'JS',
  greet: () => console.log(this.name) // undefined ❌
};
obj.greet();

7️⃣ Prototypes & Inheritance – Understand prototype chain & Object.create()
8️⃣ Call, Apply, Bind – Reuse functions across objects.
9️⃣ Currying – Convert multi-arg functions into chained unary functions.
💡 Example:

const add = a => b => c => a + b + c;
console.log(add(1)(2)(3)); // 6

⚡ Performance & Behavior

🔹 Debouncing & Throttling – Control event-heavy functions (scroll, resize).
🔹 Type Coercion – Why [] == false is true but [] === false is false.
🔹 Null vs Undefined – Subtle but vital.
🔹 Event Delegation – Handle events efficiently using bubbling.
🔹 setTimeout vs setInterval – Know their differences & clear mechanisms.
🔹 IIFE – Immediately execute isolated logic.

📦 Modern JavaScript (ES6+ Musts)

✅ Modules – import / export patterns
✅ Destructuring, Spread, Rest – Cleaner syntax, shallow copies
✅ Optional Chaining & Nullish Coalescing – Avoid “Cannot read property of undefined” errors
✅ Generators & Iterators – Lazy data loading
✅ Pure Functions & Side Effects – Functional programming essentials

🧩 Memory & Code Quality

Understand garbage collection & memory leaks

Know when to clone deeply vs shallowly (structuredClone, JSON.parse(JSON.stringify(...)))

Write predictable, testable code

🧰 Frontend Interview Blueprint