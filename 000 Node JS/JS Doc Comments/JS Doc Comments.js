// The below comments written here allows any user to show the working of the function and meaning of parameters for this method.
/**
 * Adds two numbers and returns the sum.
 * 
 * @param {number} input1 - The first number.
 * @param {number} input2 - The second number.
 * @returns {number} The sum of input1 and input2.
 */

let sum = function(input1, input2) {
    return input1 + input2;
}

// Declare variables a and b
let a = 5;
let b = 6;

// Log the sum of a and b to the console
console.log(sum(a, b));
